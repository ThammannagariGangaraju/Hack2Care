services:
  - type: web
    name: hack2care
    env: node
    buildCommand: npm install --include=dev && npm run build && npm prune --production
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
